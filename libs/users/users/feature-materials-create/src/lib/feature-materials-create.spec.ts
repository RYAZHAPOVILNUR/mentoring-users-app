import { featureMaterialsCreate } from './feature-materials-create';

describe('featureMaterialsCreate', () => {
  it('should work', () => {
    expect(featureMaterialsCreate()).toEqual('feature-materials-create');
  });
});
