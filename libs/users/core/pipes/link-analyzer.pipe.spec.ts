import { LinkAnalyzerPipe } from './link-analyzer.pipe';

describe('LinkAnalyzerPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkAnalyzerPipe();
    expect(pipe).toBeTruthy();
  });
});
