import { YouTubeLinkImgPipe } from './you-tube-link-img.pipe';

describe('YouTubeLinkImgPipe', () => {
  it('create an instance', () => {
    const pipe = new YouTubeLinkImgPipe();
    expect(pipe).toBeTruthy();
  });
});
