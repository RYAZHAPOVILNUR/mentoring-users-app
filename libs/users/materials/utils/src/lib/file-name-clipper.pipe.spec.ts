import { FileNameClipperPipe } from './file-name-clipper.pipe';

describe('FileNameClipperPipe', () => {
  it('create an instance', () => {
    const pipe = new FileNameClipperPipe();
    expect(pipe).toBeTruthy();
  });
});
