import { materialsDataAccess } from './materials-data-access';

describe('materialsDataAccess', () => {
  it('should work', () => {
    expect(materialsDataAccess()).toEqual('materials-data-access');
  });
});
