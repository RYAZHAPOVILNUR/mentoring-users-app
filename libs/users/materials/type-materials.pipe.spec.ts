import { TypeMaterialsPipe } from './type-materials.pipe';

describe('TypeMaterialsPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeMaterialsPipe();
    expect(pipe).toBeTruthy();
  });
});
