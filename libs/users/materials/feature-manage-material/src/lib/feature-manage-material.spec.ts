import { featureManageMaterial } from './feature-manage-material';

describe('featureManageMaterial', () => {
  it('should work', () => {
    expect(featureManageMaterial()).toEqual('feature-manage-material');
  });
});
