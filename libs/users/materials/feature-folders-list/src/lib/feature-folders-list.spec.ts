import { featureFoldersList } from './feature-folders-list';

describe('featureFoldersList', () => {
  it('should work', () => {
    expect(featureFoldersList()).toEqual('feature-folders-list');
  });
});
